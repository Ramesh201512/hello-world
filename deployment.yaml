apiVersion: v1
kind: Pod
metadata:
  name: hello-world-pod
spec:
  containers:
    - name: hello-world-container
      image: rameshaws2012/moonstone
      ports:
        - name: web
          containerPort: 8081
          protocol: TCP
