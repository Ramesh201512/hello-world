apiVersion: v1
kind: Pod
metadata:
  name: hello-world-pod
spec:
  containers:
    - name: hello-world-container
      image: rameshaws2012/tcs_project
      ports:
        - name: web
          containerPort: 8081
          protocol: TCP
